<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.channelsoft.alps" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.WelcomeActivity" android:label="欢迎页面"/>
        <activity android:name=".activity.RegisterActivity" android:label="注册页面"/>
        <activity android:name=".activity.Activity1" android:label="1"/>
        <activity android:name=".activity.Activity2" android:label="2"/>
        <activity android:name=".activity.Activity3" android:label="3"/>
        <activity android:name=".activity.Activity4" android:label="4"/>
        <activity android:name=".activity.CustomSoundActivity" android:label="录音页面"/>
        <activity android:name=".activity.MyActivity" android:label="测试"/>
    </application>

    <!-- 连接网络的权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!-- 录音权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- 访问网络状态 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- 获取手机状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
</manifest>
